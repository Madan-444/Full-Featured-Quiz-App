import React from 'react'

function Ans({n,place}) {
    return (
        <div>
            <button> <h4> {n}. The capital city of the Nepal is {place} </h4> </button>
        </div>
    )
}

export default Ans
